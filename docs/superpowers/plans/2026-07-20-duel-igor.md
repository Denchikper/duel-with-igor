# Дуэль с Игорем — план реализации

> **Для агентов:** этот план исполняется тремя агентами параллельно после фазы 0. Шаги помечены чекбоксами (`- [ ]`). Каждый агент работает только в своей зоне файлов и коммитит в свою ветку.

**Goal:** Telegram Mini App с асинхронными PvP-дуэлями по задачам ЕГЭ по информатике, готовый к демо на реальном устройстве к 2026-07-21 13:00.

**Architecture:** Клиентское SPA на React + TypeScript, ходит напрямую в Supabase по anon-ключу. Серверного кода нет. Дуэль — запись в `duels` с зафиксированным набором из 7 вопросов; каждый участник пишет свой `run`; экран сравнения читает два `run` по одному `duel_id`. Передача вызова — deep-link `t.me/<bot>?startapp=<duel_id>`.

**Tech Stack:** React 19, TypeScript 5, Vite 6, `@telegram-apps/sdk-react` 3.x, `@supabase/supabase-js` 2.x, Vitest, деплой на Cloudflare Pages.

## Global Constraints

- Вопросов в дуэли — ровно **7** (`QUESTIONS_PER_DUEL`).
- Лимит на вопрос — **30000 мс** (`QUESTION_TIME_LIMIT_MS`). По истечении ответ засчитывается неверным, `chosen_index: null`, `time_ms: 30000`.
- Вариантов ответа у каждого вопроса — ровно **4**.
- `src/contracts.ts` создаётся в фазе 0 и **замораживается**. Менять его может только владелец ядра (Claude) и только с оповещением остальных двух агентов. Все остальные читают, но не редактируют.
- Компоненты в `src/ui/` — **чисто презентационные**: не импортируют `supabase`, не делают сетевых запросов, не содержат игровой логики. Только пропсы на вход, колбэки на выход.
- Язык интерфейса и всего контента — русский.
- Зоны файлов не пересекаются. Claude: `src/core/`, `src/screens/`, `src/lib/`, `src/App.tsx`. Copilot: `content/`. Третий: `src/ui/`.
- TDD применяется к чистой логике (`src/core/scoring.ts`, `src/core/igor.ts`) — там она окупается. UI и экраны проверяются вручную в браузере и на устройстве: за 22 часа тесты на React-компоненты не окупаются.
- Каждый агент коммитит в свою ветку: `track/core`, `track/content`, `track/ui`. Мерж в `main` — на фазе интеграции.

---

# ФАЗА 0 — Фундамент (Claude, соло, 15:00–16:00)

Пока фаза 0 не влита в `main`, остальные два агента не начинают. Это единственная последовательная часть плана.

### Task 1: Скелет проекта и заморозка контрактов

**Files:**
- Create: `package.json`, `tsconfig.json`, `vite.config.ts`, `index.html`, `.env.example`, `.gitignore`
- Create: `src/contracts.ts`, `src/main.tsx`, `src/App.tsx`

**Interfaces:**
- Produces: весь модуль `src/contracts.ts` — типы `Question`, `Answer`, `Duel`, `Run`, `LeaderboardRow`, `IgorEvent`, `IgorLines`, константы `QUESTIONS_PER_DUEL`, `QUESTION_TIME_LIMIT_MS`, и пропсы всех UI-компонентов. Это единственный источник истины для всех трёх агентов.

- [ ] **Step 1: Инициализировать Vite-проект**

```bash
cd /Users/daniel/Development/hack-profimatika
npm create vite@latest . -- --template react-ts
npm install
npm install @supabase/supabase-js @telegram-apps/sdk-react
npm install -D vitest
```

Если `npm create` ругается на непустую папку — выбрать «Ignore files and continue».

- [ ] **Step 2: Добавить скрипт тестов в `package.json`**

В секцию `"scripts"` добавить:

```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 3: Написать `src/contracts.ts`**

```ts
// ЗАМОРОЖЕННЫЙ КОНТРАКТ. Правки — только через владельца ядра.

export const QUESTIONS_PER_DUEL = 7;
export const QUESTION_TIME_LIMIT_MS = 30_000;
export const OPTIONS_PER_QUESTION = 4;

// ---------- Данные ----------

export interface Question {
  id: string;
  topic: string;
  difficulty: 1 | 2 | 3;
  text: string;
  options: string[];      // ровно 4
  correct_index: number;  // 0..3
  explanation: string;
  igor_comment: string;
}

export interface Answer {
  question_id: string;
  chosen_index: number | null;  // null = не успел
  correct: boolean;
  time_ms: number;
}

export interface Duel {
  id: string;
  question_ids: string[];  // ровно 7, порядок значим
  created_by: number;
  created_at: string;
}

export interface Run {
  id: string;
  duel_id: string;
  tg_user_id: number;
  display_name: string;
  photo_url: string | null;
  score: number;           // 0..7
  total_time_ms: number;
  answers: Answer[];       // ровно 7, порядок совпадает с question_ids
  created_at: string;
}

export interface LeaderboardRow {
  tg_user_id: number;
  display_name: string;
  photo_url: string | null;
  score: number;
  total_time_ms: number;
}

// ---------- Игорь ----------

export type IgorEvent =
  | 'run_start'
  | 'correct'
  | 'wrong'
  | 'streak3'
  | 'fast_answer'
  | 'timeout'
  | 'duel_win'
  | 'duel_loss'
  | 'duel_draw';

export type IgorLines = Record<IgorEvent, string[]>;

export type IgorMood = 'neutral' | 'happy' | 'smug' | 'disappointed';

// ---------- Пропсы UI-компонентов ----------

export interface QuestionCardProps {
  question: Question;
  questionNumber: number;   // 1..7
  totalQuestions: number;
  selectedIndex: number | null;
  revealed: boolean;        // true = подсветить правильный/неправильный
  disabled: boolean;
  onSelect: (index: number) => void;
}

export interface TimerProps {
  remainingMs: number;
  totalMs: number;
}

export interface ProgressBarProps {
  answers: (Answer | null)[];  // длина 7, null = ещё не дошли
  currentIndex: number;        // 0..6
}

export interface IgorBubbleProps {
  line: string;
  mood: IgorMood;
}

export interface ResultCompareProps {
  questions: Question[];   // 7, в порядке дуэли
  me: Run;
  rival: Run | null;       // null = соперник ещё не сыграл
  verdict: string;         // реплика Игоря
  onShare: () => void;
  onRematch: () => void;
}

export interface LeaderboardProps {
  rows: LeaderboardRow[];
  meUserId: number | null;
}

export interface ButtonProps {
  children: React.ReactNode;
  variant: 'primary' | 'secondary';
  disabled?: boolean;
  onClick: () => void;
}
```

- [ ] **Step 4: Создать `.env.example`**

```
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_BOT_USERNAME=
VITE_APP_URL=
```

- [ ] **Step 5: Проверить, что проект собирается**

Run: `npm run build`
Expected: сборка проходит без ошибок TypeScript.

- [ ] **Step 6: Коммит**

```bash
git add -A
git commit -m "feat: scaffold vite project and freeze contracts"
```

---

### Task 2: Схема Supabase и наполнение окружения

**Files:**
- Create: `supabase/schema.sql`
- Create: `.env.local` (не коммитится)

- [ ] **Step 1: Написать `supabase/schema.sql`**

```sql
create extension if not exists "pgcrypto";

create table questions (
  id uuid primary key default gen_random_uuid(),
  topic text not null,
  difficulty int not null check (difficulty between 1 and 3),
  text text not null,
  options jsonb not null,
  correct_index int not null check (correct_index between 0 and 3),
  explanation text not null,
  igor_comment text not null
);

create table duels (
  id uuid primary key default gen_random_uuid(),
  question_ids jsonb not null,
  created_by bigint not null,
  created_at timestamptz not null default now()
);

create table runs (
  id uuid primary key default gen_random_uuid(),
  duel_id uuid not null references duels(id) on delete cascade,
  tg_user_id bigint not null,
  display_name text not null,
  photo_url text,
  score int not null check (score between 0 and 7),
  total_time_ms int not null,
  answers jsonb not null,
  created_at timestamptz not null default now()
);

create index runs_duel_id_idx on runs(duel_id);
create index runs_leaderboard_idx on runs(score desc, total_time_ms asc);

alter table questions enable row level security;
alter table duels enable row level security;
alter table runs enable row level security;

create policy "read questions" on questions for select to anon using (true);
create policy "read duels"     on duels     for select to anon using (true);
create policy "read runs"      on runs      for select to anon using (true);
create policy "create duels"   on duels     for insert to anon with check (true);
create policy "create runs"    on runs      for insert to anon with check (true);
```

Намеренно нет политик `update` и `delete` — записи неизменяемы после вставки.

- [ ] **Step 2: Создать проект в Supabase и применить схему**

Зайти на supabase.com → New project → SQL Editor → вставить содержимое `supabase/schema.sql` → Run.
Expected: «Success. No rows returned».

- [ ] **Step 3: Создать бота в BotFather**

В Telegram: `@BotFather` → `/newbot` → имя и username. Сохранить username.

- [ ] **Step 4: Заполнить `.env.local`**

Взять URL и anon-ключ в Supabase → Project Settings → API.

```
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
VITE_BOT_USERNAME=duel_igor_bot
VITE_APP_URL=https://duel-igor.pages.dev
```

- [ ] **Step 5: Убедиться, что `.env.local` в `.gitignore`**

Run: `grep -q ".env.local" .gitignore && echo OK || echo "ДОБАВИТЬ .env.local в .gitignore"`
Expected: `OK`

- [ ] **Step 6: Коммит**

```bash
git add supabase/schema.sql .gitignore
git commit -m "feat: add supabase schema with rls policies"
```

---

### Task 3: Ранний деплой и раздача репозитория

Деплой делается **до** написания фич, а не после. Причина: привязка домена к боту и проверка, что мини-апп вообще открывается в Telegram, — источник неожиданных задержек. Найти их надо сейчас, а не в 23:00.

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: Временная заглушка в `src/App.tsx`**

```tsx
export default function App() {
  return <div style={{ padding: 24, fontSize: 20 }}>Дуэль с Игорем — скоро</div>;
}
```

- [ ] **Step 2: Запушить репозиторий на GitHub**

```bash
gh repo create hack-profimatika --private --source=. --push
```

- [ ] **Step 3: Задеплоить на Cloudflare Pages**

dash.cloudflare.com → Workers & Pages → Create → Pages → Connect to Git → выбрать репозиторий.
Build command: `npm run build`. Output directory: `dist`.
В Settings → Environment variables добавить все четыре `VITE_*` переменные из `.env.local`.

- [ ] **Step 4: Привязать мини-апп к боту**

`@BotFather` → `/newapp` → выбрать бота → указать URL с Cloudflare Pages.

- [ ] **Step 5: Проверить на реальном телефоне**

Открыть `t.me/<bot>/<app>` с телефона.
Expected: видна надпись «Дуэль с Игорем — скоро». Если пусто или ошибка — чинить сейчас, дальше не идти.

- [ ] **Step 6: Дать доступ к репозиторию двум другим участникам и объявить старт**

```bash
git add -A && git commit -m "chore: deploy placeholder app"
git push
```

Оба других агента клонируют репозиторий и создают свои ветки:

```bash
git checkout -b track/content   # Copilot
git checkout -b track/ui        # третий
```

---

# ТРЕК A — Ядро (Claude, ветка `track/core`, 16:00–20:00)

### Task 4: Скоринг и сравнение забегов

**Files:**
- Create: `src/core/scoring.ts`
- Test: `src/core/scoring.test.ts`

**Interfaces:**
- Consumes: `Answer`, `Run` из `src/contracts.ts`
- Produces: `summarize(answers: Answer[]): { score: number; total_time_ms: number }`, `compareRuns(me: Run, rival: Run): 'win' | 'loss' | 'draw'`

- [ ] **Step 1: Написать падающий тест**

```ts
import { describe, it, expect } from 'vitest';
import { summarize, compareRuns } from './scoring';
import type { Answer, Run } from '../contracts';

const a = (correct: boolean, time_ms: number): Answer => ({
  question_id: 'q', chosen_index: correct ? 0 : 1, correct, time_ms,
});

const run = (score: number, total_time_ms: number): Run => ({
  id: 'r', duel_id: 'd', tg_user_id: 1, display_name: 'X', photo_url: null,
  score, total_time_ms, answers: [], created_at: '',
});

describe('summarize', () => {
  it('считает верные ответы и суммарное время', () => {
    expect(summarize([a(true, 1000), a(false, 2000), a(true, 500)]))
      .toEqual({ score: 2, total_time_ms: 3500 });
  });

  it('на пустом наборе даёт нули', () => {
    expect(summarize([])).toEqual({ score: 0, total_time_ms: 0 });
  });
});

describe('compareRuns', () => {
  it('выигрывает тот, у кого больше счёт', () => {
    expect(compareRuns(run(5, 60000), run(4, 10000))).toBe('win');
  });

  it('при равном счёте выигрывает тот, кто быстрее', () => {
    expect(compareRuns(run(5, 30000), run(5, 40000))).toBe('win');
    expect(compareRuns(run(5, 50000), run(5, 40000))).toBe('loss');
  });

  it('полное совпадение — ничья', () => {
    expect(compareRuns(run(5, 30000), run(5, 30000))).toBe('draw');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npm test`
Expected: FAIL — `Failed to resolve import "./scoring"`.

- [ ] **Step 3: Реализовать**

```ts
import type { Answer, Run } from '../contracts';

export function summarize(answers: Answer[]): { score: number; total_time_ms: number } {
  return {
    score: answers.filter((x) => x.correct).length,
    total_time_ms: answers.reduce((sum, x) => sum + x.time_ms, 0),
  };
}

export function compareRuns(me: Run, rival: Run): 'win' | 'loss' | 'draw' {
  if (me.score !== rival.score) return me.score > rival.score ? 'win' : 'loss';
  if (me.total_time_ms !== rival.total_time_ms) {
    return me.total_time_ms < rival.total_time_ms ? 'win' : 'loss';
  }
  return 'draw';
}
```

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `npm test`
Expected: PASS, 5 тестов.

- [ ] **Step 5: Коммит**

```bash
git add src/core/scoring.ts src/core/scoring.test.ts
git commit -m "feat: add run scoring and comparison"
```

---

### Task 5: Подбор реплик Игоря

**Files:**
- Create: `src/core/igor.ts`
- Test: `src/core/igor.test.ts`

**Interfaces:**
- Consumes: `IgorEvent`, `IgorLines`, `IgorMood` из `src/contracts.ts`
- Produces: `createIgor(lines: IgorLines): { say(event: IgorEvent): string }`, `moodFor(event: IgorEvent): IgorMood`

- [ ] **Step 1: Написать падающий тест**

```ts
import { describe, it, expect } from 'vitest';
import { createIgor, moodFor } from './igor';
import type { IgorLines } from '../contracts';

const lines = {
  run_start: ['старт'], correct: ['верно A', 'верно B'], wrong: ['мимо'],
  streak3: ['серия'], fast_answer: ['быстро'], timeout: ['время'],
  duel_win: ['победа'], duel_loss: ['слив'], duel_draw: ['ничья'],
} as IgorLines;

describe('createIgor', () => {
  it('возвращает реплику для события', () => {
    expect(createIgor(lines).say('wrong')).toBe('мимо');
  });

  it('не повторяет реплику подряд, если есть альтернатива', () => {
    const igor = createIgor(lines);
    const first = igor.say('correct');
    expect(igor.say('correct')).not.toBe(first);
  });

  it('на неизвестном событии не падает', () => {
    const igor = createIgor({ ...lines, correct: [] } as IgorLines);
    expect(igor.say('correct')).toBe('');
  });
});

describe('moodFor', () => {
  it('сопоставляет событию настроение', () => {
    expect(moodFor('correct')).toBe('happy');
    expect(moodFor('wrong')).toBe('disappointed');
    expect(moodFor('duel_win')).toBe('smug');
    expect(moodFor('run_start')).toBe('neutral');
  });
});
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `npm test`
Expected: FAIL — модуль не найден.

- [ ] **Step 3: Реализовать**

```ts
import type { IgorEvent, IgorLines, IgorMood } from '../contracts';

const MOODS: Record<IgorEvent, IgorMood> = {
  run_start: 'neutral',
  correct: 'happy',
  wrong: 'disappointed',
  streak3: 'happy',
  fast_answer: 'happy',
  timeout: 'disappointed',
  duel_win: 'smug',
  duel_loss: 'disappointed',
  duel_draw: 'neutral',
};

export function moodFor(event: IgorEvent): IgorMood {
  return MOODS[event] ?? 'neutral';
}

export function createIgor(lines: IgorLines) {
  const last: Partial<Record<IgorEvent, string>> = {};

  return {
    say(event: IgorEvent): string {
      const pool = lines[event] ?? [];
      if (pool.length === 0) return '';
      const choices = pool.length > 1 ? pool.filter((x) => x !== last[event]) : pool;
      const picked = choices[Math.floor(Math.random() * choices.length)];
      last[event] = picked;
      return picked;
    },
  };
}
```

- [ ] **Step 4: Убедиться, что тесты проходят**

Run: `npm test`
Expected: PASS, 9 тестов суммарно.

- [ ] **Step 5: Коммит**

```bash
git add src/core/igor.ts src/core/igor.test.ts
git commit -m "feat: add igor line picker"
```

---

### Task 6: Клиенты Supabase и Telegram

**Files:**
- Create: `src/lib/supabase.ts`, `src/lib/telegram.ts`

**Interfaces:**
- Produces: `supabase` (клиент), `getTelegramUser(): TgUser`, `getStartParam(): string | null`, `shareDuel(duelId: string, score: number, timeMs: number): void`, `haptic(type: 'success' | 'error'): void`

- [ ] **Step 1: Написать `src/lib/supabase.ts`**

```ts
import { createClient } from '@supabase/supabase-js';

export const supabase = createClient(
  import.meta.env.VITE_SUPABASE_URL,
  import.meta.env.VITE_SUPABASE_ANON_KEY,
);
```

- [ ] **Step 2: Написать `src/lib/telegram.ts`**

Работаем через `window.Telegram.WebApp` напрямую: у SDK-обёрток чаще ломается инициализация, а нам нужна предсказуемость. Fallback-значения нужны, чтобы приложение открывалось в обычном браузере при разработке.

```ts
export interface TgUser {
  id: number;
  name: string;
  photo_url: string | null;
}

interface TgWebApp {
  initDataUnsafe?: {
    user?: { id: number; first_name?: string; last_name?: string; photo_url?: string };
    start_param?: string;
  };
  ready(): void;
  expand(): void;
  openTelegramLink(url: string): void;
  HapticFeedback?: { notificationOccurred(type: 'success' | 'error' | 'warning'): void };
}

function webApp(): TgWebApp | null {
  return (window as unknown as { Telegram?: { WebApp: TgWebApp } }).Telegram?.WebApp ?? null;
}

export function initTelegram(): void {
  const app = webApp();
  if (!app) return;
  app.ready();
  app.expand();
}

export function getTelegramUser(): TgUser {
  const user = webApp()?.initDataUnsafe?.user;
  if (!user) return { id: 0, name: 'Гость', photo_url: null };
  const name = [user.first_name, user.last_name].filter(Boolean).join(' ') || 'Игрок';
  return { id: user.id, name, photo_url: user.photo_url ?? null };
}

export function getStartParam(): string | null {
  const fromTg = webApp()?.initDataUnsafe?.start_param;
  if (fromTg) return fromTg;
  return new URLSearchParams(window.location.search).get('duel');
}

export function shareDuel(duelId: string, score: number, timeMs: number): void {
  const bot = import.meta.env.VITE_BOT_USERNAME;
  const link = `https://t.me/${bot}?startapp=${duelId}`;
  const seconds = (timeMs / 1000).toFixed(1);
  const text = `Я выбил ${score}/7 за ${seconds}с у Игоря Линькова. Слабо повторить?`;
  const url = `https://t.me/share/url?url=${encodeURIComponent(link)}&text=${encodeURIComponent(text)}`;
  const app = webApp();
  if (app) app.openTelegramLink(url);
  else window.open(url, '_blank');
}

export function haptic(type: 'success' | 'error'): void {
  webApp()?.HapticFeedback?.notificationOccurred(type);
}
```

- [ ] **Step 3: Подключить скрипт Telegram в `index.html`**

В `<head>` добавить строку перед подключением `main.tsx`:

```html
<script src="https://telegram.org/js/telegram-web-app.js"></script>
```

- [ ] **Step 4: Проверить сборку**

Run: `npm run build`
Expected: успех.

- [ ] **Step 5: Коммит**

```bash
git add src/lib index.html
git commit -m "feat: add supabase and telegram clients"
```

---

### Task 7: Слой доступа к данным

**Files:**
- Create: `src/core/api.ts`

**Interfaces:**
- Consumes: `supabase` из `src/lib/supabase.ts`; типы из `src/contracts.ts`
- Produces: `loadQuestions()`, `pickDuelQuestions(all)`, `createDuel(questionIds, userId)`, `loadDuel(duelId)`, `submitRun(input)`, `loadLeaderboard(limit)`

- [ ] **Step 1: Написать `src/core/api.ts`**

Все 50 вопросов грузятся одним запросом и кешируются в модуле: набор маленький, а так исчезает целый класс проблем с выборкой по списку id.

```ts
import { supabase } from '../lib/supabase';
import { QUESTIONS_PER_DUEL } from '../contracts';
import type { Answer, Duel, LeaderboardRow, Question, Run } from '../contracts';

let cache: Question[] | null = null;

export async function loadQuestions(): Promise<Question[]> {
  if (cache) return cache;
  const { data, error } = await supabase.from('questions').select('*');
  if (error) throw error;
  cache = data as Question[];
  return cache;
}

export function pickDuelQuestions(all: Question[]): Question[] {
  const shuffled = [...all].sort(() => Math.random() - 0.5);
  return shuffled.slice(0, QUESTIONS_PER_DUEL);
}

export async function createDuel(questionIds: string[], userId: number): Promise<Duel> {
  const { data, error } = await supabase
    .from('duels')
    .insert({ question_ids: questionIds, created_by: userId })
    .select()
    .single();
  if (error) throw error;
  return data as Duel;
}

export async function loadDuel(duelId: string): Promise<{ duel: Duel; questions: Question[]; runs: Run[] }> {
  const [{ data: duel, error: duelError }, { data: runs, error: runsError }, all] = await Promise.all([
    supabase.from('duels').select('*').eq('id', duelId).single(),
    supabase.from('runs').select('*').eq('duel_id', duelId).order('created_at'),
    loadQuestions(),
  ]);
  if (duelError) throw duelError;
  if (runsError) throw runsError;

  const byId = new Map(all.map((q) => [q.id, q]));
  const questions = (duel as Duel).question_ids
    .map((id) => byId.get(id))
    .filter((q): q is Question => Boolean(q));

  return { duel: duel as Duel, questions, runs: (runs ?? []) as Run[] };
}

export async function submitRun(input: {
  duel_id: string;
  tg_user_id: number;
  display_name: string;
  photo_url: string | null;
  score: number;
  total_time_ms: number;
  answers: Answer[];
}): Promise<Run> {
  const { data, error } = await supabase.from('runs').insert(input).select().single();
  if (error) throw error;
  return data as Run;
}

export async function loadLeaderboard(limit = 20): Promise<LeaderboardRow[]> {
  const { data, error } = await supabase
    .from('runs')
    .select('tg_user_id, display_name, photo_url, score, total_time_ms')
    .order('score', { ascending: false })
    .order('total_time_ms', { ascending: true })
    .limit(200);
  if (error) throw error;

  const best = new Map<number, LeaderboardRow>();
  for (const row of (data ?? []) as LeaderboardRow[]) {
    if (!best.has(row.tg_user_id)) best.set(row.tg_user_id, row);
  }
  return [...best.values()].slice(0, limit);
}
```

`loadLeaderboard` берёт 200 строк и схлопывает по игроку в памяти — так у каждого игрока в таблице ровно одна, его лучшая, запись без группировки на стороне БД.

- [ ] **Step 2: Проверить сборку**

Run: `npm run build`
Expected: успех.

- [ ] **Step 3: Коммит**

```bash
git add src/core/api.ts
git commit -m "feat: add data access layer"
```

---

### Task 8: Движок забега

**Files:**
- Create: `src/core/useRun.ts`

**Interfaces:**
- Consumes: `QUESTION_TIME_LIMIT_MS`, `Answer`, `Question` из `src/contracts.ts`; `summarize` из `src/core/scoring.ts`
- Produces: хук `useRun(questions: Question[], onFinish: (answers: Answer[]) => void)`, возвращающий `{ current, index, answers, selectedIndex, revealed, remainingMs, lastEvent, select }`

- [ ] **Step 1: Написать `src/core/useRun.ts`**

После ответа карточка на 1.5 секунды показывает правильный вариант и реплику Игоря, затем переходит к следующему вопросу. Без этой паузы игрок не успевает ничему научиться, и «полезность» из брифа теряется.

```ts
import { useCallback, useEffect, useRef, useState } from 'react';
import { QUESTION_TIME_LIMIT_MS } from '../contracts';
import type { Answer, IgorEvent, Question } from '../contracts';

const REVEAL_MS = 1500;
const FAST_ANSWER_MS = 5000;

export function useRun(questions: Question[], onFinish: (answers: Answer[]) => void) {
  const [index, setIndex] = useState(0);
  const [answers, setAnswers] = useState<Answer[]>([]);
  const [selectedIndex, setSelectedIndex] = useState<number | null>(null);
  const [revealed, setRevealed] = useState(false);
  const [remainingMs, setRemainingMs] = useState(QUESTION_TIME_LIMIT_MS);
  const [lastEvent, setLastEvent] = useState<IgorEvent>('run_start');
  const startedAt = useRef(Date.now());
  const answersRef = useRef<Answer[]>([]);

  const commit = useCallback((chosen: number | null) => {
    const question = questions[index];
    if (!question) return;

    const elapsed = chosen === null
      ? QUESTION_TIME_LIMIT_MS
      : Math.min(Date.now() - startedAt.current, QUESTION_TIME_LIMIT_MS);
    const correct = chosen === question.correct_index;

    const answer: Answer = {
      question_id: question.id,
      chosen_index: chosen,
      correct,
      time_ms: elapsed,
    };

    const next = [...answersRef.current, answer];
    answersRef.current = next;
    setAnswers(next);
    setSelectedIndex(chosen);
    setRevealed(true);

    const streak = next.slice(-3);
    if (chosen === null) setLastEvent('timeout');
    else if (!correct) setLastEvent('wrong');
    else if (streak.length === 3 && streak.every((x) => x.correct)) setLastEvent('streak3');
    else if (elapsed < FAST_ANSWER_MS) setLastEvent('fast_answer');
    else setLastEvent('correct');

    setTimeout(() => {
      if (next.length >= questions.length) {
        onFinish(next);
        return;
      }
      setIndex((i) => i + 1);
      setSelectedIndex(null);
      setRevealed(false);
      setRemainingMs(QUESTION_TIME_LIMIT_MS);
      startedAt.current = Date.now();
    }, REVEAL_MS);
  }, [index, questions, onFinish]);

  useEffect(() => {
    if (revealed || questions.length === 0) return;
    const tick = setInterval(() => {
      const left = QUESTION_TIME_LIMIT_MS - (Date.now() - startedAt.current);
      if (left <= 0) {
        setRemainingMs(0);
        commit(null);
      } else {
        setRemainingMs(left);
      }
    }, 100);
    return () => clearInterval(tick);
  }, [revealed, questions.length, commit]);

  const select = useCallback((option: number) => {
    if (revealed) return;
    commit(option);
  }, [revealed, commit]);

  return {
    current: questions[index] ?? null,
    index,
    answers,
    selectedIndex,
    revealed,
    remainingMs,
    lastEvent,
    select,
  };
}
```

- [ ] **Step 2: Проверить сборку**

Run: `npm run build`
Expected: успех.

- [ ] **Step 3: Коммит**

```bash
git add src/core/useRun.ts
git commit -m "feat: add run engine hook"
```

---

### Task 9: Экраны и роутинг

**Files:**
- Create: `src/screens/HomeScreen.tsx`, `GameScreen.tsx`, `ResultScreen.tsx`, `ChallengeScreen.tsx`, `CompareScreen.tsx`, `LeaderboardScreen.tsx`
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: всё из `src/core/`, `src/lib/`, компоненты из `src/ui/` по пропсам из `src/contracts.ts`
- Produces: работающее приложение

Роутер не подключается: состояний шесть, и `useState` со строковым тегом надёжнее и быстрее, чем настройка history-режима внутри Telegram WebView.

- [ ] **Step 1: Написать `src/App.tsx`**

```tsx
import { useEffect, useState } from 'react';
import { initTelegram, getStartParam } from './lib/telegram';
import { loadQuestions } from './core/api';
import HomeScreen from './screens/HomeScreen';
import GameScreen from './screens/GameScreen';
import ResultScreen from './screens/ResultScreen';
import ChallengeScreen from './screens/ChallengeScreen';
import CompareScreen from './screens/CompareScreen';
import LeaderboardScreen from './screens/LeaderboardScreen';
import type { Question, Run } from './contracts';

export type Screen =
  | { name: 'home' }
  | { name: 'challenge'; duelId: string }
  | { name: 'game'; duelId: string; questions: Question[] }
  | { name: 'result'; duelId: string; run: Run }
  | { name: 'compare'; duelId: string; meRunId: string }
  | { name: 'leaderboard' };

export default function App() {
  const [screen, setScreen] = useState<Screen>({ name: 'home' });
  const [ready, setReady] = useState(false);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    initTelegram();
    loadQuestions()
      .then(() => {
        const duelId = getStartParam();
        if (duelId) setScreen({ name: 'challenge', duelId });
        setReady(true);
      })
      .catch((e) => setError(String(e?.message ?? e)));
  }, []);

  if (error) return <div style={{ padding: 24 }}>Не удалось загрузить: {error}</div>;
  if (!ready) return <div style={{ padding: 24 }}>Игорь готовит задачи…</div>;

  switch (screen.name) {
    case 'home':        return <HomeScreen go={setScreen} />;
    case 'challenge':   return <ChallengeScreen duelId={screen.duelId} go={setScreen} />;
    case 'game':        return <GameScreen duelId={screen.duelId} questions={screen.questions} go={setScreen} />;
    case 'result':      return <ResultScreen duelId={screen.duelId} run={screen.run} go={setScreen} />;
    case 'compare':     return <CompareScreen duelId={screen.duelId} meRunId={screen.meRunId} go={setScreen} />;
    case 'leaderboard': return <LeaderboardScreen go={setScreen} />;
  }
}
```

- [ ] **Step 2: Написать `src/screens/GameScreen.tsx`**

```tsx
import { useCallback, useMemo } from 'react';
import { useRun } from '../core/useRun';
import { summarize } from '../core/scoring';
import { createIgor, moodFor } from '../core/igor';
import { submitRun } from '../core/api';
import { getTelegramUser, haptic } from '../lib/telegram';
import QuestionCard from '../ui/QuestionCard';
import Timer from '../ui/Timer';
import ProgressBar from '../ui/ProgressBar';
import IgorBubble from '../ui/IgorBubble';
import igorLines from '../../content/igor-lines.json';
import { QUESTIONS_PER_DUEL } from '../contracts';
import type { Answer, IgorLines, Question } from '../contracts';
import type { Screen } from '../App';

export default function GameScreen({ duelId, questions, go }: {
  duelId: string;
  questions: Question[];
  go: (s: Screen) => void;
}) {
  const igor = useMemo(() => createIgor(igorLines as IgorLines), []);

  const finish = useCallback(async (answers: Answer[]) => {
    const user = getTelegramUser();
    const { score, total_time_ms } = summarize(answers);
    const run = await submitRun({
      duel_id: duelId,
      tg_user_id: user.id,
      display_name: user.name,
      photo_url: user.photo_url,
      score,
      total_time_ms,
      answers,
    });
    go({ name: 'result', duelId, run });
  }, [duelId, go]);

  const run = useRun(questions, finish);
  if (!run.current) return <div style={{ padding: 24 }}>Загрузка…</div>;

  const line = igor.say(run.lastEvent);
  if (run.revealed) haptic(run.answers[run.answers.length - 1]?.correct ? 'success' : 'error');

  const slots: (Answer | null)[] = Array.from(
    { length: QUESTIONS_PER_DUEL },
    (_, i) => run.answers[i] ?? null,
  );

  return (
    <div>
      <ProgressBar answers={slots} currentIndex={run.index} />
      <Timer remainingMs={run.remainingMs} totalMs={30_000} />
      <IgorBubble line={line} mood={moodFor(run.lastEvent)} />
      <QuestionCard
        question={run.current}
        questionNumber={run.index + 1}
        totalQuestions={QUESTIONS_PER_DUEL}
        selectedIndex={run.selectedIndex}
        revealed={run.revealed}
        disabled={run.revealed}
        onSelect={run.select}
      />
    </div>
  );
}
```

- [ ] **Step 3: Написать `src/screens/HomeScreen.tsx`**

```tsx
import { useState } from 'react';
import { loadQuestions, pickDuelQuestions, createDuel } from '../core/api';
import { getTelegramUser } from '../lib/telegram';
import Button from '../ui/Button';
import IgorBubble from '../ui/IgorBubble';
import type { Screen } from '../App';

export default function HomeScreen({ go }: { go: (s: Screen) => void }) {
  const [busy, setBusy] = useState(false);

  async function start() {
    setBusy(true);
    try {
      const all = await loadQuestions();
      const questions = pickDuelQuestions(all);
      const duel = await createDuel(questions.map((q) => q.id), getTelegramUser().id);
      go({ name: 'game', duelId: duel.id, questions });
    } finally {
      setBusy(false);
    }
  }

  return (
    <div style={{ padding: 16 }}>
      <IgorBubble line="Ну что, проверим, кто тут готов к ЕГЭ?" mood="smug" />
      <Button variant="primary" disabled={busy} onClick={start}>Бросить вызов</Button>
      <Button variant="secondary" onClick={() => go({ name: 'leaderboard' })}>Таблица рекордов</Button>
    </div>
  );
}
```

- [ ] **Step 4: Написать `src/screens/ResultScreen.tsx`**

```tsx
import { shareDuel } from '../lib/telegram';
import Button from '../ui/Button';
import IgorBubble from '../ui/IgorBubble';
import type { Run } from '../contracts';
import type { Screen } from '../App';

export default function ResultScreen({ duelId, run, go }: {
  duelId: string; run: Run; go: (s: Screen) => void;
}) {
  const seconds = (run.total_time_ms / 1000).toFixed(1);
  return (
    <div style={{ padding: 16 }}>
      <IgorBubble line={`${run.score} из 7 за ${seconds} секунд. Теперь найди того, кто сделает лучше.`} mood="neutral" />
      <Button variant="primary" onClick={() => shareDuel(duelId, run.score, run.total_time_ms)}>
        Вызвать друга
      </Button>
      <Button variant="secondary" onClick={() => go({ name: 'compare', duelId, meRunId: run.id })}>
        Разбор ответов
      </Button>
      <Button variant="secondary" onClick={() => go({ name: 'home' })}>На главную</Button>
    </div>
  );
}
```

- [ ] **Step 5: Написать `src/screens/ChallengeScreen.tsx`**

```tsx
import { useEffect, useState } from 'react';
import { loadDuel } from '../core/api';
import Button from '../ui/Button';
import IgorBubble from '../ui/IgorBubble';
import type { Question, Run } from '../contracts';
import type { Screen } from '../App';

export default function ChallengeScreen({ duelId, go }: { duelId: string; go: (s: Screen) => void }) {
  const [rival, setRival] = useState<Run | null>(null);
  const [questions, setQuestions] = useState<Question[]>([]);

  useEffect(() => {
    loadDuel(duelId).then(({ questions, runs }) => {
      setQuestions(questions);
      setRival(runs[0] ?? null);
    });
  }, [duelId]);

  if (!rival) return <div style={{ padding: 24 }}>Открываю вызов…</div>;

  const seconds = (rival.total_time_ms / 1000).toFixed(1);
  return (
    <div style={{ padding: 16 }}>
      <IgorBubble
        line={`Тебя вызвал ${rival.display_name}: ${rival.score} из 7 за ${seconds} секунд. Слабо повторить?`}
        mood="smug"
      />
      <Button variant="primary" onClick={() => go({ name: 'game', duelId, questions })}>
        Принять вызов
      </Button>
    </div>
  );
}
```

- [ ] **Step 6: Написать `src/screens/CompareScreen.tsx`**

```tsx
import { useEffect, useState } from 'react';
import { loadDuel } from '../core/api';
import { compareRuns } from '../core/scoring';
import { createIgor } from '../core/igor';
import { shareDuel } from '../lib/telegram';
import ResultCompare from '../ui/ResultCompare';
import igorLines from '../../content/igor-lines.json';
import type { IgorLines, Question, Run } from '../contracts';
import type { Screen } from '../App';

export default function CompareScreen({ duelId, meRunId, go }: {
  duelId: string; meRunId: string; go: (s: Screen) => void;
}) {
  const [state, setState] = useState<{ questions: Question[]; me: Run; rival: Run | null } | null>(null);

  useEffect(() => {
    loadDuel(duelId).then(({ questions, runs }) => {
      const me = runs.find((r) => r.id === meRunId);
      if (!me) return;
      setState({ questions, me, rival: runs.find((r) => r.id !== meRunId) ?? null });
    });
  }, [duelId, meRunId]);

  if (!state) return <div style={{ padding: 24 }}>Считаю…</div>;

  const igor = createIgor(igorLines as IgorLines);
  const verdict = state.rival
    ? igor.say(({ win: 'duel_win', loss: 'duel_loss', draw: 'duel_draw' } as const)[
        compareRuns(state.me, state.rival)
      ])
    : 'Соперник ещё не играл. Поторопи его.';

  return (
    <ResultCompare
      questions={state.questions}
      me={state.me}
      rival={state.rival}
      verdict={verdict}
      onShare={() => shareDuel(duelId, state.me.score, state.me.total_time_ms)}
      onRematch={() => go({ name: 'home' })}
    />
  );
}
```

- [ ] **Step 7: Написать `src/screens/LeaderboardScreen.tsx`**

```tsx
import { useEffect, useState } from 'react';
import { loadLeaderboard } from '../core/api';
import { getTelegramUser } from '../lib/telegram';
import Leaderboard from '../ui/Leaderboard';
import Button from '../ui/Button';
import type { LeaderboardRow } from '../contracts';
import type { Screen } from '../App';

export default function LeaderboardScreen({ go }: { go: (s: Screen) => void }) {
  const [rows, setRows] = useState<LeaderboardRow[]>([]);
  useEffect(() => { loadLeaderboard().then(setRows); }, []);

  return (
    <div>
      <Leaderboard rows={rows} meUserId={getTelegramUser().id} />
      <Button variant="secondary" onClick={() => go({ name: 'home' })}>Назад</Button>
    </div>
  );
}
```

- [ ] **Step 8: Проверить сборку и коммит**

Run: `npm run build`
Expected: успех при условии, что ветки `track/ui` и `track/content` уже влиты. До интеграции сборка будет ругаться на отсутствующие `src/ui/*` и `content/*` — это ожидаемо.

```bash
git add src/screens src/App.tsx
git commit -m "feat: add screens and navigation"
```

---

# ТРЕК B — Контент (Copilot, ветка `track/content`, 16:00–20:00)

Зона: только каталог `content/`. Ничего за его пределами не трогать.

### Task 10: Банк реплик Игоря

**Files:**
- Create: `content/igor-lines.json`

**Interfaces:**
- Consumes: тип `IgorLines` из `src/contracts.ts` (читать, не менять)
- Produces: JSON-объект, импортируемый ядром как `igorLines`

- [ ] **Step 1: Создать `content/igor-lines.json`**

Ровно девять ключей, ни одного лишнего, ни одного пропущенного. В каждом массиве **минимум 6 реплик**. Реплики короткие — до 90 символов, иначе не помещаются в пузырь на экране телефона. Тон: ироничный препод, который подкалывает, но не унижает.

```json
{
  "run_start": [
    "Погнали. Семь задач, тридцать секунд на каждую.",
    "Так, телефон отложил, думаем головой.",
    "Начали. И без калькулятора, я слежу."
  ],
  "correct": [
    "Верно. Уже неплохо.",
    "Засчитано.",
    "Правильно. Не расслабляйся."
  ],
  "wrong": [
    "Мимо. Читай условие внимательнее.",
    "Нет. Классическая ошибка, кстати.",
    "Не то. Разберём в конце."
  ],
  "streak3": [
    "Три подряд. Начинаю уважать.",
    "Серия. Вот так и надо.",
    "Три из трёх. Кто-то готовился."
  ],
  "fast_answer": [
    "Быстро. И, что удивительно, верно.",
    "Меньше пяти секунд. Уважаю.",
    "Мгновенно. Значит, тему знаешь."
  ],
  "timeout": [
    "Время вышло. На экзамене его тоже не добавят.",
    "Не успел. Бывает, но не должно.",
    "Тридцать секунд прошло. Идём дальше."
  ],
  "duel_win": [
    "Ты выиграл. Соперник пусть тренируется.",
    "Победа. Заслуженно.",
    "Ты сильнее. Пока что."
  ],
  "duel_loss": [
    "Проиграл. Реванш никто не отменял.",
    "Соперник оказался быстрее. Обидно.",
    "Ты слабее в этот раз. В этот раз."
  ],
  "duel_draw": [
    "Ничья. Такое я вижу нечасто.",
    "Ровно. Придётся переигрывать.",
    "Один в один. Разбирайтесь сами."
  ]
}
```

Приведённые реплики — стартовый набор по 3 штуки. **Дополнить каждый массив до 6 и более**, сохраняя тон и длину.

- [ ] **Step 2: Проверить, что JSON валиден**

Run: `node -e "const l=require('./content/igor-lines.json');const k=['run_start','correct','wrong','streak3','fast_answer','timeout','duel_win','duel_loss','duel_draw'];const bad=k.filter(x=>!Array.isArray(l[x])||l[x].length<6);console.log(bad.length?'МАЛО РЕПЛИК: '+bad:'OK, ключей '+Object.keys(l).length)"`
Expected: `OK, ключей 9`

- [ ] **Step 3: Коммит**

```bash
git add content/igor-lines.json
git commit -m "content: add igor line bank"
```

---

### Task 11: Банк вопросов ЕГЭ

**Files:**
- Create: `content/questions.json`

**Interfaces:**
- Consumes: тип `Question` из `src/contracts.ts` (читать, не менять). Поле `id` в JSON **не указывается** — его генерирует база.
- Produces: массив объектов для загрузки в таблицу `questions`

- [ ] **Step 1: Создать `content/questions.json`**

Формат — массив объектов. Требования, нарушение любого из них ломает приложение:

- `options` — **ровно 4 строки**
- `correct_index` — число 0–3, индекс верного варианта в `options`
- `difficulty` — 1, 2 или 3
- `explanation` — почему верен именно этот вариант, 1–3 предложения, по существу
- `igor_comment` — короткая реплика Игоря именно про эту задачу, до 90 символов
- Вопрос должен решаться в уме за 30 секунд. Задачи на длинные вычисления не подходят.

```json
[
  {
    "topic": "Системы счисления",
    "difficulty": 1,
    "text": "Сколько единиц в двоичной записи числа 42?",
    "options": ["2", "3", "4", "5"],
    "correct_index": 1,
    "explanation": "42 в двоичной системе — 101010. Единиц в записи три.",
    "igor_comment": "Переводить в двоичную надо уметь на автомате."
  },
  {
    "topic": "Логика",
    "difficulty": 2,
    "text": "При каком значении X выражение (X > 3) И (X < 7) ложно?",
    "options": ["4", "5", "6", "7"],
    "correct_index": 3,
    "explanation": "При X = 7 второе условие X < 7 ложно, значит вся конъюнкция ложна.",
    "igor_comment": "Строгое неравенство — самая частая ловушка."
  }
]
```

**Набрать не менее 50 вопросов** с распределением по темам:

| Тема | Количество |
|---|---|
| Системы счисления | 8 |
| Логика и таблицы истинности | 8 |
| Кодирование информации и объём данных | 7 |
| Алгоритмы и исполнители | 7 |
| Python: результат выполнения фрагмента | 8 |
| Графы и поиск кратчайшего пути | 6 |
| Таблицы истинности и запросы к БД | 6 |

Распределение сложности по всему набору примерно: 40% difficulty 1, 40% difficulty 2, 20% difficulty 3.

- [ ] **Step 2: Проверить корректность всех вопросов**

Run:

```bash
node -e "
const q = require('./content/questions.json');
const bad = q.filter(x =>
  !Array.isArray(x.options) || x.options.length !== 4 ||
  typeof x.correct_index !== 'number' || x.correct_index < 0 || x.correct_index > 3 ||
  ![1,2,3].includes(x.difficulty) ||
  !x.text || !x.explanation || !x.igor_comment || !x.topic
);
console.log('всего: ' + q.length + ', битых: ' + bad.length);
if (bad.length) console.log(JSON.stringify(bad.slice(0,3), null, 2));
"
```

Expected: `всего: 50, битых: 0` (или больше 50). Если битых не ноль — чинить, пока не ноль.

- [ ] **Step 3: Коммит**

```bash
git add content/questions.json
git commit -m "content: add 50 ege questions"
```

---

### Task 12: Сид-скрипт загрузки в Supabase

**Files:**
- Create: `content/seed.mjs`

**Interfaces:**
- Consumes: `content/questions.json`, переменные окружения из `.env.local`
- Produces: заполненную таблицу `questions`

- [ ] **Step 1: Написать `content/seed.mjs`**

```js
import { createClient } from '@supabase/supabase-js';
import { readFileSync } from 'node:fs';

const url = process.env.VITE_SUPABASE_URL;
const key = process.env.VITE_SUPABASE_ANON_KEY;

if (!url || !key) {
  console.error('Нет VITE_SUPABASE_URL или VITE_SUPABASE_ANON_KEY в окружении.');
  process.exit(1);
}

const questions = JSON.parse(readFileSync('./content/questions.json', 'utf8'));
const supabase = createClient(url, key);

const { data, error } = await supabase.from('questions').insert(questions).select('id');

if (error) {
  console.error('Ошибка загрузки:', error.message);
  process.exit(1);
}

console.log(`Загружено вопросов: ${data.length}`);
```

- [ ] **Step 2: Запустить сид**

Run: `set -a && source .env.local && set +a && node content/seed.mjs`
Expected: `Загружено вопросов: 50`

Если ошибка про RLS — политика `insert` на `questions` отсутствует намеренно. Загрузить вопросы через SQL Editor в Supabase или временно добавить политику, а после загрузки удалить её.

- [ ] **Step 3: Коммит**

```bash
git add content/seed.mjs
git commit -m "content: add supabase seed script"
```

---

# ТРЕК C — Интерфейс (третий агент, ветка `track/ui`, 16:00–20:00)

Зона: только каталог `src/ui/`. Читать `src/contracts.ts`, но не редактировать.

**Железное правило трека:** ни один файл в `src/ui/` не импортирует `supabase`, не делает `fetch` и не содержит игровой логики. Данные приходят пропсами, события уходят колбэками. Это позволяет разрабатывать весь интерфейс, пока ядро ещё не готово.

### Task 13: Моки и песочница

**Files:**
- Create: `src/ui/mocks.ts`, `src/ui/Sandbox.tsx`

**Interfaces:**
- Consumes: типы из `src/contracts.ts`
- Produces: `mockQuestion`, `mockAnswers`, `mockRun`, `mockRival`, `mockLeaderboard` — данные для разработки компонентов

- [ ] **Step 1: Написать `src/ui/mocks.ts`**

```ts
import type { Answer, LeaderboardRow, Question, Run } from '../contracts';

export const mockQuestion: Question = {
  id: 'q1',
  topic: 'Системы счисления',
  difficulty: 1,
  text: 'Сколько единиц в двоичной записи числа 42?',
  options: ['2', '3', '4', '5'],
  correct_index: 1,
  explanation: '42 в двоичной системе — 101010. Единиц три.',
  igor_comment: 'Переводить в двоичную надо уметь на автомате.',
};

export const mockAnswers: Answer[] = Array.from({ length: 7 }, (_, i) => ({
  question_id: `q${i + 1}`,
  chosen_index: i % 3 === 0 ? 1 : 0,
  correct: i % 3 !== 0,
  time_ms: 3000 + i * 900,
}));

export const mockRun: Run = {
  id: 'run-me', duel_id: 'd1', tg_user_id: 1, display_name: 'Даня', photo_url: null,
  score: 5, total_time_ms: 38400, answers: mockAnswers, created_at: '',
};

export const mockRival: Run = {
  id: 'run-rival', duel_id: 'd1', tg_user_id: 2, display_name: 'Игорь Л.', photo_url: null,
  score: 6, total_time_ms: 31200,
  answers: mockAnswers.map((a, i) => ({ ...a, correct: i !== 4, time_ms: a.time_ms - 700 })),
  created_at: '',
};

export const mockLeaderboard: LeaderboardRow[] = [
  { tg_user_id: 2, display_name: 'Игорь Л.', photo_url: null, score: 7, total_time_ms: 28900 },
  { tg_user_id: 1, display_name: 'Даня', photo_url: null, score: 5, total_time_ms: 38400 },
  { tg_user_id: 3, display_name: 'Анастасия', photo_url: null, score: 5, total_time_ms: 41100 },
];
```

- [ ] **Step 2: Написать `src/ui/Sandbox.tsx`**

Экран, на котором видны все компоненты сразу. Пока ядро не готово, разработка идёт здесь: временно поменять в `src/main.tsx` рендер `<App />` на `<Sandbox />`, и вернуть обратно перед интеграцией.

```tsx
import { useState } from 'react';
import QuestionCard from './QuestionCard';
import Timer from './Timer';
import ProgressBar from './ProgressBar';
import IgorBubble from './IgorBubble';
import Leaderboard from './Leaderboard';
import ResultCompare from './ResultCompare';
import Button from './Button';
import { mockQuestion, mockAnswers, mockRun, mockRival, mockLeaderboard } from './mocks';

export default function Sandbox() {
  const [selected, setSelected] = useState<number | null>(null);
  const [revealed, setRevealed] = useState(false);

  return (
    <div style={{ display: 'grid', gap: 32, padding: 16 }}>
      <ProgressBar answers={[...mockAnswers.slice(0, 3), null, null, null, null]} currentIndex={3} />
      <Timer remainingMs={12400} totalMs={30000} />
      <IgorBubble line="Мимо. Читай условие внимательнее." mood="disappointed" />
      <QuestionCard
        question={mockQuestion}
        questionNumber={4}
        totalQuestions={7}
        selectedIndex={selected}
        revealed={revealed}
        disabled={revealed}
        onSelect={(i) => { setSelected(i); setRevealed(true); }}
      />
      <Button variant="primary" onClick={() => { setSelected(null); setRevealed(false); }}>
        Сбросить карточку
      </Button>
      <Leaderboard rows={mockLeaderboard} meUserId={1} />
      <ResultCompare
        questions={Array.from({ length: 7 }, (_, i) => ({ ...mockQuestion, id: `q${i + 1}` }))}
        me={mockRun}
        rival={mockRival}
        verdict="Проиграл. Реванш никто не отменял."
        onShare={() => {}}
        onRematch={() => {}}
      />
    </div>
  );
}
```

- [ ] **Step 3: Коммит**

```bash
git add src/ui/mocks.ts src/ui/Sandbox.tsx
git commit -m "ui: add mocks and component sandbox"
```

---

### Task 14: Базовые компоненты

**Files:**
- Create: `src/ui/theme.css`, `src/ui/Button.tsx`, `src/ui/IgorBubble.tsx`, `src/ui/Timer.tsx`, `src/ui/ProgressBar.tsx`

**Interfaces:**
- Consumes: `ButtonProps`, `IgorBubbleProps`, `TimerProps`, `ProgressBarProps` из `src/contracts.ts`
- Produces: default-экспорт компонента из каждого файла

Визуальное направление — от айдентики хакатона: тёмный фон, кислотно-зелёный акцент (`#B4FF39`), фиолетовый вторичный (`#8B5CF6`), моноширинный шрифт в акцентах, сетка/терминальная эстетика. Цвета фона и текста брать из CSS-переменных Telegram (`--tg-theme-bg-color`, `--tg-theme-text-color`) с падением на тёмную тему.

- [ ] **Step 1: Написать `src/ui/theme.css`**

```css
:root {
  --bg: var(--tg-theme-bg-color, #0d0f0a);
  --text: var(--tg-theme-text-color, #eaffd0);
  --muted: #7c8a63;
  --accent: #b4ff39;
  --secondary: #8b5cf6;
  --danger: #ff5c5c;
  --card: rgba(180, 255, 57, 0.06);
  --border: rgba(180, 255, 57, 0.25);
}

body {
  margin: 0;
  background: var(--bg);
  color: var(--text);
  font-family: -apple-system, system-ui, sans-serif;
}
```

Импортировать в `src/main.tsx`: `import './ui/theme.css';`

- [ ] **Step 2: Написать `src/ui/Button.tsx`**

```tsx
import type { ButtonProps } from '../contracts';

export default function Button({ children, variant, disabled, onClick }: ButtonProps) {
  const primary = variant === 'primary';
  return (
    <button
      onClick={onClick}
      disabled={disabled}
      style={{
        width: '100%',
        padding: '16px',
        marginTop: 12,
        borderRadius: 14,
        border: primary ? 'none' : '1px solid var(--border)',
        background: primary ? 'var(--accent)' : 'transparent',
        color: primary ? '#0d0f0a' : 'var(--text)',
        fontSize: 17,
        fontWeight: 700,
        opacity: disabled ? 0.5 : 1,
      }}
    >
      {children}
    </button>
  );
}
```

- [ ] **Step 3: Написать `src/ui/IgorBubble.tsx`**

Пузырь с репликой и аватаром Игоря. Настроение меняет цвет рамки: `happy` — акцентный зелёный, `disappointed` — красный, `smug` — фиолетовый, `neutral` — серый. Реплика меняется часто, поэтому смена текста анимируется коротким fade — иначе текст «дёргается».

- [ ] **Step 4: Написать `src/ui/Timer.tsx`**

Горизонтальная полоса, заполнение `remainingMs / totalMs`. Ниже 5 секунд — красная и пульсирует. Числовое значение секунд моноширинным шрифтом.

- [ ] **Step 5: Написать `src/ui/ProgressBar.tsx`**

Семь сегментов в ряд. Отвеченные верно — зелёные, неверно — красные, текущий — подсвечен рамкой, будущие — приглушённые.

- [ ] **Step 6: Проверить в песочнице**

Run: `npm run dev`, открыть в браузере с включённой мобильной эмуляцией (ширина 390px).
Expected: все четыре компонента видны и не разъезжаются.

- [ ] **Step 7: Коммит**

```bash
git add src/ui
git commit -m "ui: add theme and base components"
```

---

### Task 15: Карточка вопроса

**Files:**
- Create: `src/ui/QuestionCard.tsx`

**Interfaces:**
- Consumes: `QuestionCardProps` из `src/contracts.ts`
- Produces: default-экспорт `QuestionCard`

Это самый важный компонент: он на экране бóльшую часть демо.

- [ ] **Step 1: Написать `src/ui/QuestionCard.tsx`**

Требования к поведению:

- Шапка: «Вопрос N из 7» и тема мелким моноширинным шрифтом.
- Текст вопроса — крупно, не менее 18px, с сохранением переносов строк (`white-space: pre-wrap`), потому что во фрагментах кода на Python значимы отступы.
- Четыре варианта — крупные кнопки в столбик, минимальная высота касания 48px.
- При `revealed === false` вариант при нажатии вызывает `onSelect(index)`.
- При `revealed === true`: верный вариант всегда подсвечен зелёным; если `selectedIndex` не совпал с верным — выбранный подсвечен красным; под вариантами появляется блок `explanation`.
- При `disabled === true` нажатия игнорируются.
- `selectedIndex === null` при `revealed === true` означает истёкшее время: подсветить верный вариант и показать разбор, ничего не помечая красным.

- [ ] **Step 2: Проверить все состояния в песочнице**

Прокликать: невыбранное, выбранное верно, выбранное неверно. Проверить длинный вопрос с многострочным кодом — вёрстка не должна ломаться.

- [ ] **Step 3: Коммит**

```bash
git add src/ui/QuestionCard.tsx
git commit -m "ui: add question card"
```

---

### Task 16: Экран сравнения и таблица рекордов

**Files:**
- Create: `src/ui/ResultCompare.tsx`, `src/ui/Leaderboard.tsx`

**Interfaces:**
- Consumes: `ResultCompareProps`, `LeaderboardProps` из `src/contracts.ts`
- Produces: default-экспорты `ResultCompare` и `Leaderboard`

`ResultCompare` — кульминация демо. Именно его показывают на 3-й минуте питча, и он должен читаться с проектора за пять секунд.

- [ ] **Step 1: Написать `src/ui/ResultCompare.tsx`**

Требования:

- Шапка: две колонки с именами и итогом «5/7 · 38.4с» против «6/7 · 31.2с». Победитель подсвечен акцентным цветом.
- Пузырь с `verdict` под шапкой.
- Таблица из 7 строк по числу вопросов. В каждой строке: номер, галочка или крестик для каждого игрока, время каждого в секундах. Тот, кто ответил быстрее **и верно**, помечен акцентом.
- Строка раскрывается по нажатию: показывает текст вопроса, верный ответ и `explanation`. Это и есть «полезность» приложения — разбор ошибок сразу после дуэли.
- Если `rival === null`: колонка соперника показывает «ещё не играл», таблица показывает только свои ответы, кнопка «Вызвать друга» выведена на первый план.
- Внизу две кнопки: `onShare` («Вызвать ещё») и `onRematch` («Новая дуэль»).

- [ ] **Step 2: Написать `src/ui/Leaderboard.tsx`**

Требования: нумерованный список, аватар (при `photo_url === null` — кружок с первой буквой имени), имя, счёт и время. Строка, где `tg_user_id === meUserId`, подсвечена и подписана «ты». Первые три места — с медалями.

- [ ] **Step 3: Проверить в песочнице оба состояния `ResultCompare`**

Проверить с `rival={mockRival}` и с `rival={null}`.

- [ ] **Step 4: Коммит**

```bash
git add src/ui/ResultCompare.tsx src/ui/Leaderboard.tsx
git commit -m "ui: add compare screen and leaderboard"
```

---

# ФАЗА 2 — Интеграция (все вместе, 20:00–22:00)

### Task 17: Слияние веток и сквозная сборка

- [ ] **Step 1: Влить все три ветки в `main`**

```bash
git checkout main
git merge track/ui
git merge track/content
git merge track/core
```

Конфликтов быть не должно: зоны файлов не пересекаются. Если конфликт возник — значит кто-то вышел за свою зону, разбираться поимённо.

- [ ] **Step 2: Вернуть рендер `App` в `src/main.tsx`**

Убедиться, что рендерится `<App />`, а не `<Sandbox />`.

- [ ] **Step 3: Прогнать тесты и сборку**

Run: `npm test && npm run build`
Expected: все тесты проходят, сборка успешна.

- [ ] **Step 4: Коммит и пуш**

```bash
git add -A
git commit -m "feat: integrate core, ui and content"
git push
```

---

### Task 18: Сквозной прогон на реальном устройстве

Это главная проверка проекта. Пока она не пройдена, ничего не считается сделанным.

- [ ] **Step 1: Дождаться деплоя Cloudflare Pages и открыть мини-апп на телефоне**

- [ ] **Step 2: Пройти полный цикл на двух аккаунтах**

1. Аккаунт A: открыть → «Бросить вызов» → пройти 7 вопросов → увидеть результат.
2. Аккаунт A: «Вызвать друга» → отправить ссылку аккаунту B.
3. Аккаунт B: открыть ссылку → увидеть экран вызова с именем и результатом A → пройти те же вопросы.
4. Аккаунт B: увидеть экран сравнения с корректными данными обоих.
5. Открыть таблицу рекордов — оба игрока в ней.

- [ ] **Step 3: Записать все найденные дефекты списком и починить по убыванию влияния на демо**

Приоритет: сначала то, что видно в 90-секундном демо, потом остальное.

- [ ] **Step 4: Коммит фиксов**

```bash
git add -A
git commit -m "fix: issues found in device testing"
git push
```

---

# ФАЗА 3 — Полировка и стретч (22:00–01:00)

### Task 19: Призрак соперника

Берётся в работу **только если Task 18 пройдена полностью**. Даёт ощущение realtime без единого websocket: во время своего забега виден прогресс соперника, воспроизведённый по уже записанным в `runs.answers` таймингам.

**Files:**
- Modify: `src/screens/GameScreen.tsx`
- Create: `src/ui/GhostBar.tsx`

**Interfaces:**
- Consumes: `Run` из `src/contracts.ts`
- Produces: `GhostBar` с пропсами `{ rival: Run; elapsedMs: number }`

- [ ] **Step 1: Создать `src/ui/GhostBar.tsx`**

Компонент считает, на каком вопросе был бы соперник к моменту `elapsedMs`: накапливает `time_ms` его ответов, пока сумма не превысит `elapsedMs`. Рисует полосу с аватаром соперника в этой позиции и подписью «Игорь Л. — вопрос 4».

- [ ] **Step 2: Расширить вариант `game` в типе `Screen`**

В `src/App.tsx` вариант `game` получает соперника. Это единственная правка навигационного типа после фазы 0:

```ts
| { name: 'game'; duelId: string; questions: Question[]; rival: Run | null }
```

Соответственно `HomeScreen` передаёт `rival: null` (свой забег, соперника ещё нет), а `ChallengeScreen` — `rival` из уже загруженных им `runs[0]`.

- [ ] **Step 3: Передать соперника и время от старта в `GameScreen`**

`GameScreen` принимает `rival: Run | null` и держит `elapsedMs` от начала забега. При `rival === null` `GhostBar` не рендерится.

- [ ] **Step 4: Проверить на устройстве, что призрак двигается и не тормозит забег**

- [ ] **Step 5: Коммит**

```bash
git add -A
git commit -m "feat: add rival ghost progress"
git push
```

---

### Task 20: Финальная полировка

- [ ] **Step 1: Проверить пустые и ошибочные состояния**

Открытие битой ссылки-вызова, пустая таблица рекордов, отсутствие сети. Ни один сценарий не должен показывать белый экран.

- [ ] **Step 2: Проверить на светлой теме Telegram**

Переключить тему в клиенте Telegram. Текст должен оставаться читаемым.

- [ ] **Step 3: Заполнить карточку бота**

В BotFather: описание, картинка, короткое описание. Это первое, что видит жюри при открытии.

- [ ] **Step 4: Коммит и финальный деплой**

```bash
git add -A
git commit -m "polish: empty states and bot profile"
git push
```

---

# ФАЗА 4 — Питч (09:00–13:00)

### Task 21: Подготовка выступления

- [ ] **Step 1: Собрать слайды под тайминг из брифа**

0:00–0:30 название и одно предложение. 0:30–1:30 проблема. 1:30–3:00 живое демо. 3:00–4:00 польза и фишки. 4:00–5:00 итог и Q&A.

- [ ] **Step 2: Заготовить демо-состояние заранее**

До выхода на сцену создать дуэль с хорошим результатом и держать ссылку наготове. На демо: показать свой забег на 3–4 вопросах, отправить вызов **члену жюри в зал**, открыть экран сравнения. Живой вызов жюри — сильнейшие 20 секунд всего выступления, их нельзя отдавать на волю случая.

- [ ] **Step 3: Заготовить ответы на вопросы Q&A**

- Про валидацию `initData`: признать, что подпись не проверяется, назвать решение — Edge Function с проверкой HMAC перед вставкой `run`.
- Про накрутку результатов: `update` и `delete` запрещены политиками, запись неизменяема; полная защита — перенос проверки ответов на сервер.
- Про контент: 50 вопросов сейчас, схема позволяет расширять без изменений кода.

- [ ] **Step 4: Прогнать питч под таймер дважды**

Первый прогон покажет перебор по времени. Второй — что сокращение сработало.

- [ ] **Step 5: Финальная проверка, что прод открывается с телефона**

За 15 минут до выступления. С мобильного интернета, а не с вайфая площадки.

---

## Проверка плана на покрытие спеки

| Требование спеки | Задача |
|---|---|
| Цикл: забег → шеринг → второй игрок → сравнение | Tasks 8, 9, 18 |
| Стек React + TS + Vite + Supabase | Task 1 |
| Три таблицы, индексы, RLS без update/delete | Task 2 |
| Deep-link `startapp` и нативный шеринг | Task 6 |
| Шесть экранов | Task 9 |
| Скоринг, лимит 30с, тай-брейк по времени | Tasks 4, 8 |
| Контракты заморожены до параллельной работы | Task 1 |
| Разделение зон между тремя агентами | Треки A, B, C |
| 50+ вопросов с объяснениями | Task 11 |
| Реплики Игоря на 9 событий | Tasks 5, 10 |
| Граница UI/ядро, разработка против моков | Tasks 13–16 |
| Прогон на устройстве в 20:00 | Task 18 |
| Стретч: призрак соперника | Task 19 |
| Деплой на HTTPS, привязка к боту | Task 3 |
| Питч в 5 минут с репетицией | Task 21 |
